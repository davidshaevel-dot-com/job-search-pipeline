# Simplified Search Criteria for Phase 1 Testing
#
# The complex nested structure in search-criteria.yaml will be used in Phase 3
# For now, we use a simple flat structure that works with the current JSearch adapter

search:
  keywords:
    - "DevOps"

  location: ""

  # Optional: remote filter (not yet implemented in JSearch adapter)
  # remote: true

  # Optional: employment type (not yet implemented in JSearch adapter)
  # employment_type: "FULLTIME"
